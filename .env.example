ANALYZER_BACKEND=gemini

# --- Gemini (ANALYZER_BACKEND=gemini のとき必須) ---
GEMINI_API_KEY=replace_me
GEMINI_MODEL=gemini-pro-vision
GEMINI_MAX_TOKENS=1024
GEMINI_TEMPERATURE=0.4

# 429（クオータ/レート制限）対策
GEMINI_MAX_RETRIES=5
GEMINI_RETRY_BUFFER_SECONDS=0.5
# 例: free tier 5 req/min の場合は 12 を推奨
GEMINI_REQUEST_SPACING_SECONDS=0

# --- Local LLM (ANALYZER_BACKEND=local) / LM Studio ---
# OpenAI互換API: http://localhost:1234/v1
LOCAL_LLM_BASE_URL=http://localhost:1234/v1
# LM Studio側のモデル名。未指定/auto/local-model の場合は /v1/models から自動選択
LOCAL_LLM_MODEL=auto
LOCAL_LLM_MAX_TOKENS=1024
LOCAL_LLM_TEMPERATURE=0.4
LOCAL_LLM_TIMEOUT_SECONDS=60
NANOBANANA_API_KEY=
NANOBANANA_ENDPOINT=https://api.nanobanana.pro/v1/images
NANOBANANA_MODEL=nano-pro-vision
ENABLE_VISUALIZATION=false
TIMEZONE=Asia/Tokyo
CAPTURE_INTERVAL_SECONDS=60
IDLE_THRESHOLD_MINUTES=5

# --- データ保存先（デフォルト: EXE配置場所/<PC名>/...） ---
# 通常は設定不要。デフォルトで以下の構造になります：
#   <mirulog-observer.exeの場所>/<COMPUTERNAME>/captures/
#   <mirulog-observer.exeの場所>/<COMPUTERNAME>/archive/
#   <mirulog-observer.exeの場所>/<COMPUTERNAME>/logs/
#   <mirulog-observer.exeの場所>/<COMPUTERNAME>/mirulog.db
#
# 明示的にパスを指定する場合のみコメントを外してください：
# CAPTURE_ROOT=D:/MyData/captures
# ARCHIVE_ROOT=D:/MyData/archive
# LOG_DIR=D:/MyData/logs
#
# アプリケーションルートを明示指定（通常は不要）：
# MIRULOG_ROOT=D:/MyMiruLog
#
# PC名を明示指定（通常は自動取得されるので不要）：
# MIRULOG_COMPUTERNAME=MyCustomPCName

# --- Analyzer専用設定 ---
# Analyzerが参照するデータルート（複数PCのフォルダを含む親ディレクトリ）
# デフォルト: アプリケーションルート（EXE配置場所）
# 集約PCで実行する場合、共有フォルダのルートパスを指定：
# ANALYZER_DATA_ROOT=\\server\share\mirulog

DELETE_CAPTURE_AFTER_ANALYSIS=true
SUMMARY_OUTPUT_DIR=output
REPORT_EXPORT_DIR=reports
LOG_LEVEL=INFO
DATA_RETENTION_DAYS=7